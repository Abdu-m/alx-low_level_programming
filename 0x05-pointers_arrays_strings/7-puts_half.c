#include "main.h"
/**
 * puts_half - print half of string
 * @str: pointer
 * Return: void
*/
void puts_half(char *str)
{
	int i, last;

	for ( i = 0; str[i] != '\0'; i++)
	{
		continue;
	}
	last + (i + 1) / 2;
	for (i = last; str[i]; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
